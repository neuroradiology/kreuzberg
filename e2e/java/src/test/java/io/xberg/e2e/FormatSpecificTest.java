// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c39dc9eda1096e5908f5d3623329d139e738e2f60f478cd21a1d3d23c7132388
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package io.xberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.Xberg;
import io.xberg.ExtractInput;
import io.xberg.ExtractionConfig;
import java.util.Optional;
import io.xberg.JsonUtil;

/** E2e tests for category: format_specific. */
public class FormatSpecificTest {

    @Test
    void testFormatDocxStandalone() throws Exception {
        // Standalone DOCX extraction using extract_bytes_sync
        var input = JsonUtil.fromJson("{\"data\":\"docx/fake.docx\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.wordprocessingml.document\"}", ExtractionConfig.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
assertTrue(result.content().length() >= 20, "expected length >= 20");

    }


    @Test
    void testFormatHwpxStandalone() throws Exception {
        // Standalone HWPX extraction using extract_bytes_sync
        var input = JsonUtil.fromJson("{\"data\":\"hwpx/simple.hwpx\",\"mime_type\":\"application/haansofthwpx\"}", ExtractionConfig.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
assertTrue(result.content().length() >= 20, "expected length >= 20");assertTrue(result.content().contains("Hello from HWPX"), "expected to contain: " + "Hello from HWPX");

    }


    @Test
    void testFormatPdfText() throws Exception {
        // Standalone PDF text extraction using extract_bytes_sync
        var input = JsonUtil.fromJson("{\"data\":\"pdf/fake_memo.pdf\",\"mime_type\":\"application/pdf\"}", ExtractionConfig.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
assertTrue(result.content().length() >= 50, "expected length >= 50");assertTrue(result.content().contains("Mallori") || result.content().contains("May"), "expected to contain at least one of the specified values");

    }


    @Test
    void testFormatPptx() throws Exception {
        // PPTX presentation extraction using extract_file_sync
        var input = JsonUtil.fromJson("{\"mime_type\":\"application/vnd.openxmlformats-officedocument.presentationml.presentation\",\"path\":\"pptx/simple.pptx\"}", ExtractionConfig.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());


    }


    @Test
    void testFormatXlsx() throws Exception {
        // XLSX spreadsheet extraction using extract_file_sync
        var input = JsonUtil.fromJson("{\"mime_type\":\"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet\",\"path\":\"xlsx/stanley_cups.xlsx\"}", ExtractionConfig.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());


    }

}
