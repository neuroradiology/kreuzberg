// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c39dc9eda1096e5908f5d3623329d139e738e2f60f478cd21a1d3d23c7132388
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: smoke

use xberg::extract;
use xberg::ExtractInput;

#[tokio::test]
async fn test_ocr_image_png() {
    // OCR: PNG image extraction with OCR enabled. In WASM this exercises the Uint8Array bridge parameter and Promise await in the generated OcrBackend bridge.
    let input_json: serde_json::Value =
        serde_json::from_str(r#"{"config":{},"data":"images/test_hello_world.png","mime_type":"image/png"}"#).unwrap();
    let input = serde_json::from_value::<Vec<ExtractInput>>(input_json).unwrap();
    let config_json: serde_json::Value = serde_json::from_str(r#"{}"#).unwrap();
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract(input, &config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.to_string().as_str().trim(),
        r#"image/png"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 1,
        "expected length >= 1, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"Hello"#)
            || result.content.contains(r#"World"#)
            || result.content.contains(r#"hello"#)
            || result.content.contains(r#"world"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_smoke_docx_basic() {
    // Smoke test: DOCX with formatted text
    let input_json: serde_json::Value = serde_json::from_str(r#"{"config":{},"mime_type":"application/vnd.openxmlformats-officedocument.wordprocessingml.document","path":"docx/fake.docx"}"#).unwrap();
    let input = serde_json::from_value::<Vec<ExtractInput>>(input_json).unwrap();
    let config_json: serde_json::Value = serde_json::from_str(r#"{}"#).unwrap();
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract(input, &config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.to_string().as_str().trim(),
        r#"application/vnd.openxmlformats-officedocument.wordprocessingml.document"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 20,
        "expected length >= 20, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"Lorem"#)
            || result.content.contains(r#"ipsum"#)
            || result.content.contains(r#"document"#)
            || result.content.contains(r#"text"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_smoke_html_basic() {
    // Smoke test: HTML table extraction
    let input_json: serde_json::Value =
        serde_json::from_str(r#"{"config":{},"mime_type":"text/html","path":"html/simple_table.html"}"#).unwrap();
    let input = serde_json::from_value::<Vec<ExtractInput>>(input_json).unwrap();
    let config_json: serde_json::Value = serde_json::from_str(r#"{}"#).unwrap();
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract(input, &config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.to_string().as_str().trim(),
        r#"text/html"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"Sample Data Table"#)
            || result.content.contains(r#"Laptop"#)
            || result.content.contains(r#"Electronics"#)
            || result.content.contains(r#"Product"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_smoke_image_png() {
    // Smoke test: PNG image (without OCR, metadata only)
    let input_json: serde_json::Value =
        serde_json::from_str(r#"{"config":{"disable_ocr":true},"media_type":"image/png","path":"images/sample.png"}"#)
            .unwrap();
    let input = serde_json::from_value::<Vec<ExtractInput>>(input_json).unwrap();
    let config_json: serde_json::Value = serde_json::from_str(r#"{"disable_ocr":true}"#).unwrap();
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract(input, &config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.to_string().as_str().trim(),
        r#"image/png"#,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_smoke_json_basic() {
    // Smoke test: JSON file extraction
    let input_json: serde_json::Value =
        serde_json::from_str(r#"{"config":{},"mime_type":"application/json","path":"json/simple.json"}"#).unwrap();
    let input = serde_json::from_value::<Vec<ExtractInput>>(input_json).unwrap();
    let config_json: serde_json::Value = serde_json::from_str(r#"{}"#).unwrap();
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract(input, &config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.to_string().as_str().trim(),
        r#"application/json"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_smoke_pdf_basic() {
    // Smoke test: PDF with simple text extraction
    let input_json: serde_json::Value =
        serde_json::from_str(r#"{"config":{},"mime_type":"application/pdf","path":"pdf/fake_memo.pdf"}"#).unwrap();
    let input = serde_json::from_value::<Vec<ExtractInput>>(input_json).unwrap();
    let config_json: serde_json::Value = serde_json::from_str(r#"{}"#).unwrap();
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract(input, &config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.to_string().as_str().trim(),
        r#"application/pdf"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 50,
        "expected length >= 50, got {}",
        result.content.len()
    );
    assert!(
        result.content.contains(r#"May 5, 2023"#) || result.content.contains(r#"To Whom it May Concern"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_smoke_txt_basic() {
    // Smoke test: Plain text file
    let input_json: serde_json::Value =
        serde_json::from_str(r#"{"config":{},"mime_type":"text/plain","path":"text/report.txt"}"#).unwrap();
    let input = serde_json::from_value::<Vec<ExtractInput>>(input_json).unwrap();
    let config_json: serde_json::Value = serde_json::from_str(r#"{}"#).unwrap();
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract(input, &config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.to_string().as_str().trim(),
        r#"text/plain"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 5,
        "expected length >= 5, got {}",
        result.content.len()
    );
}

#[tokio::test]
async fn test_smoke_xlsx_basic() {
    // Smoke test: XLSX with basic spreadsheet data including tables
    let input_json: serde_json::Value = serde_json::from_str(r#"{"config":{},"mime_type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","path":"xlsx/stanley_cups.xlsx"}"#).unwrap();
    let input = serde_json::from_value::<Vec<ExtractInput>>(input_json).unwrap();
    let config_json: serde_json::Value = serde_json::from_str(r#"{}"#).unwrap();
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract(input, &config).await.expect("should succeed");
    assert_eq!(
        result.mime_type.to_string().as_str().trim(),
        r#"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"#,
        "equals assertion failed"
    );
    assert!(
        result.content.len() >= 100,
        "expected length >= 100, got {}",
        result.content.len()
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Team"#),
        "expected to contain: {}",
        r#"Team"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Location"#),
        "expected to contain: {}",
        r#"Location"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Stanley Cups"#),
        "expected to contain: {}",
        r#"Stanley Cups"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Blues"#),
        "expected to contain: {}",
        r#"Blues"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Flyers"#),
        "expected to contain: {}",
        r#"Flyers"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"Maple Leafs"#),
        "expected to contain: {}",
        r#"Maple Leafs"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"STL"#),
        "expected to contain: {}",
        r#"STL"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"PHI"#),
        "expected to contain: {}",
        r#"PHI"#
    );
    assert!(
        format!("{:?}", result.content).contains(r#"TOR"#),
        "expected to contain: {}",
        r#"TOR"#
    );
    assert!(!result.tables.is_empty(), "expected >= 1");
    assert!(
        result
            .metadata
            .format
            .as_ref()
            .unwrap()
            .excel()
            .as_ref()
            .unwrap()
            .sheet_count
            .unwrap_or(0)
            >= 2,
        "expected >= 2"
    );
    assert!(
        format!(
            "{:?}",
            result
                .metadata
                .format
                .as_ref()
                .unwrap()
                .excel()
                .as_ref()
                .unwrap()
                .sheet_names
        )
        .contains(r#"Stanley Cups"#),
        "expected to contain: {}",
        r#"Stanley Cups"#
    );
}
