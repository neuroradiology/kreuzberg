<?php
// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c39dc9eda1096e5908f5d3623329d139e738e2f60f478cd21a1d3d23c7132388
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code


declare(strict_types=1);

namespace Xberg\E2e;

use PHPUnit\Framework\TestCase;
use Xberg\Xberg;
use Xberg\ExtractInput;
use Xberg\ExtractionConfig;

/** E2e tests for category: smoke. */
final class SmokeTest extends TestCase
{

    /** OCR: PNG image extraction with OCR enabled. In WASM this exercises the Uint8Array bridge parameter and Promise await in the generated OcrBackend bridge. */
    public function test_ocr_image_png(): void
    {
        $input = \Xberg\ExtractionConfig::from_json(json_encode(["config" => [], "data" => "images/test_hello_world.png", "mimeType" => "image/png"]));
        $config = \Xberg\ExtractionConfig::from_json('{}');
        $result = Xberg::extract($input, $config);

            $this->assertEquals("image/png", trim($result->mimeType));
            $this->assertGreaterThanOrEqual(1, strlen($result->content));
            $found = false;
    if (str_contains($result->content, "Hello")) { $found = true; }
    if (str_contains($result->content, "World")) { $found = true; }
    if (str_contains($result->content, "hello")) { $found = true; }
    if (str_contains($result->content, "world")) { $found = true; }
    $this->assertTrue($found, 'expected to contain at least one of the specified values');


    }


    /** Smoke test: DOCX with formatted text */
    public function test_smoke_docx_basic(): void
    {
        $input = \Xberg\ExtractionConfig::from_json(json_encode(["config" => [], "mimeType" => "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "path" => "docx/fake.docx"]));
        $config = \Xberg\ExtractionConfig::from_json('{}');
        $result = Xberg::extract($input, $config);

            $this->assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", trim($result->mimeType));
            $this->assertGreaterThanOrEqual(20, strlen($result->content));
            $found = false;
    if (str_contains($result->content, "Lorem")) { $found = true; }
    if (str_contains($result->content, "ipsum")) { $found = true; }
    if (str_contains($result->content, "document")) { $found = true; }
    if (str_contains($result->content, "text")) { $found = true; }
    $this->assertTrue($found, 'expected to contain at least one of the specified values');


    }


    /** Smoke test: HTML table extraction */
    public function test_smoke_html_basic(): void
    {
        $input = \Xberg\ExtractionConfig::from_json(json_encode(["config" => [], "mimeType" => "text/html", "path" => "html/simple_table.html"]));
        $config = \Xberg\ExtractionConfig::from_json('{}');
        $result = Xberg::extract($input, $config);

            $this->assertEquals("text/html", trim($result->mimeType));
            $this->assertGreaterThanOrEqual(10, strlen($result->content));
            $found = false;
    if (str_contains($result->content, "Sample Data Table")) { $found = true; }
    if (str_contains($result->content, "Laptop")) { $found = true; }
    if (str_contains($result->content, "Electronics")) { $found = true; }
    if (str_contains($result->content, "Product")) { $found = true; }
    $this->assertTrue($found, 'expected to contain at least one of the specified values');


    }


    /** Smoke test: PNG image (without OCR, metadata only) */
    public function test_smoke_image_png(): void
    {
        $input = \Xberg\ExtractionConfig::from_json(json_encode(["config" => ["disableOcr" => true], "mediaType" => "image/png", "path" => "images/sample.png"]));
        $config = \Xberg\ExtractionConfig::from_json(json_encode(["disableOcr" => true]));
        $result = Xberg::extract($input, $config);

            $this->assertEquals("image/png", trim($result->mimeType));


    }


    /** Smoke test: JSON file extraction */
    public function test_smoke_json_basic(): void
    {
        $input = \Xberg\ExtractionConfig::from_json(json_encode(["config" => [], "mimeType" => "application/json", "path" => "json/simple.json"]));
        $config = \Xberg\ExtractionConfig::from_json('{}');
        $result = Xberg::extract($input, $config);

            $this->assertEquals("application/json", trim($result->mimeType));
            $this->assertGreaterThanOrEqual(5, strlen($result->content));


    }


    /** Smoke test: PDF with simple text extraction */
    public function test_smoke_pdf_basic(): void
    {
        $input = \Xberg\ExtractionConfig::from_json(json_encode(["config" => [], "mimeType" => "application/pdf", "path" => "pdf/fake_memo.pdf"]));
        $config = \Xberg\ExtractionConfig::from_json('{}');
        $result = Xberg::extract($input, $config);

            $this->assertEquals("application/pdf", trim($result->mimeType));
            $this->assertGreaterThanOrEqual(50, strlen($result->content));
            $found = false;
    if (str_contains($result->content, "May 5, 2023")) { $found = true; }
    if (str_contains($result->content, "To Whom it May Concern")) { $found = true; }
    $this->assertTrue($found, 'expected to contain at least one of the specified values');


    }


    /** Smoke test: Plain text file */
    public function test_smoke_txt_basic(): void
    {
        $input = \Xberg\ExtractionConfig::from_json(json_encode(["config" => [], "mimeType" => "text/plain", "path" => "text/report.txt"]));
        $config = \Xberg\ExtractionConfig::from_json('{}');
        $result = Xberg::extract($input, $config);

            $this->assertEquals("text/plain", trim($result->mimeType));
            $this->assertGreaterThanOrEqual(5, strlen($result->content));


    }


    /** Smoke test: XLSX with basic spreadsheet data including tables */
    public function test_smoke_xlsx_basic(): void
    {
        $input = \Xberg\ExtractionConfig::from_json(json_encode(["config" => [], "mimeType" => "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "path" => "xlsx/stanley_cups.xlsx"]));
        $config = \Xberg\ExtractionConfig::from_json('{}');
        $result = Xberg::extract($input, $config);
        $tables = $result->getTables();

            $this->assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", trim($result->mimeType));
            $this->assertGreaterThanOrEqual(100, strlen($result->content));
            $this->assertStringContainsString("Team", $result->content);
    $this->assertStringContainsString("Location", $result->content);
    $this->assertStringContainsString("Stanley Cups", $result->content);
    $this->assertStringContainsString("Blues", $result->content);
    $this->assertStringContainsString("Flyers", $result->content);
    $this->assertStringContainsString("Maple Leafs", $result->content);
    $this->assertStringContainsString("STL", $result->content);
    $this->assertStringContainsString("PHI", $result->content);
    $this->assertStringContainsString("TOR", $result->content);
            $this->assertGreaterThanOrEqual(1, count($tables));
        // skipped: field 'metadata.format.excel.sheet_count' not available on result type        // skipped: field 'metadata.format.excel.sheet_names' not available on result type

    }

}
