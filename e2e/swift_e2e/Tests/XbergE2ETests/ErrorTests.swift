// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:38239aba5298b05bb7627da7dd29f61167e18c420fcb64676bf355b0040d0a32
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Xberg
import RustBridge

/// E2e tests for category: error.
final class ErrorTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testErrorEmptyBytes() async throws {
        // Graceful handling of empty bytes (should not error)
        let result = try await Xberg.extract([], "{}")
    }

    func testErrorEmptyMime() async throws {
        // Error when extracting with empty MIME type
        do {
            _ = try await Xberg.extract([], "{}")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorExtractInputConflictingOcr() async throws {
        // extract force+disable OCR
        do {
            _ = try await Xberg.extract([], "{}")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorInvalidMimeFormat() async throws {
        // Error when extracting with invalid MIME type format
        do {
            _ = try await Xberg.extract([], "{}")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorUnsupportedMime() async throws {
        // Error when extracting with unsupported MIME type
        do {
            _ = try await Xberg.extract([], "{}")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

}
