# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:2aa70876db291249caef7e2a16872fadb624b2104d7a088ca1880f8c5c3791ad
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: rerank

test_that("rerank_basic: rerank: basic plugin-based reranking with registered mock backend", {
  result <- rerank_async(query = "what is machine learning?", documents = c("Machine learning is a subset of artificial intelligence focused on learning from data.", "Cats are independent animals that prefer to play alone.", "Deep learning uses neural networks with multiple layers."), config = RerankerConfig$from_json(jsonlite::toJSON(list("model" = list("name" = "rerank_basic", "type" = "plugin")), auto_unbox = TRUE)))
  expect_true(TRUE)
})

test_that("rerank_empty_documents: rerank: empty documents list returns empty result", {
  result <- rerank_async(query = "test query", documents = character(0), config = RerankerConfig$from_json(jsonlite::toJSON(list("model" = list("name" = "rerank_empty_documents", "type" = "plugin")), auto_unbox = TRUE)))
  expect_true(TRUE)
})

test_that("rerank_top_k: rerank: top_k truncation to 2 results from 4 documents", {
  result <- rerank_async(query = "machine learning", documents = c("Machine learning is a subset of artificial intelligence focused on learning from data.", "Deep learning uses neural networks with multiple layers.", "Classical statistics predates modern machine learning.", "Photosynthesis is the process by which plants convert sunlight to energy."), config = RerankerConfig$from_json(jsonlite::toJSON(list("model" = list("name" = "rerank_top_k", "type" = "plugin"), "top_k" = 2), auto_unbox = TRUE)))
  expect_true(TRUE)
})
