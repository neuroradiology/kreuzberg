# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:9f5cdb69a320a3fc76700a4072117f85c368e32b4ec942f5edec9583c358db22
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: embeddings

test_that("embed_texts_different_preset: embed_texts: multilingual preset", {
  result <- embed_texts(texts = c("hello world", "test"), config = list("embedding_model" = "multilingual"))
  expect_gte(length(result), 2)
})

test_that("get_embedding_preset_known: get_embedding_preset: known preset", {
  result <- get_embedding_preset(preset_name = "default")
})

test_that("get_embedding_preset_nominal: get_embedding_preset: nominal case", {
  result <- get_embedding_preset(preset_name = "default")
})

test_that("get_embedding_preset_unknown: get_embedding_preset: unknown preset fails", {
  result <- get_embedding_preset(preset_name = "nonexistent-xyz")
  expect_equal(result, "")
})

test_that("list_embedding_presets_sanity: list_embedding_presets: returns at least one", {
  result <- list_embedding_presets(list())
  expect_true(if (is.character(result)) nchar(result) > 0 else length(result) > 0)
})
