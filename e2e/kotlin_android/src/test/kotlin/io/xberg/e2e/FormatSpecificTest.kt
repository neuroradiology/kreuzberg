// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c39dc9eda1096e5908f5d3623329d139e738e2f60f478cd21a1d3d23c7132388
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.Xberg
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import io.xberg.ExtractionConfig
import io.xberg.ExtractInput

/** E2e tests for category: format_specific. */
class FormatSpecificTest {

    companion object {
        init {
            try {
                System.loadLibrary("xberg_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load xberg_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testFormatDocxStandalone() = runBlocking {
        // Standalone DOCX extraction using extract_bytes_sync
        val input = MAPPER.readValue("{\"data\":\"docx/fake.docx\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.wordprocessingml.document\"}", ExtractionConfig::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
        assertTrue(result.content.length >= 20, "expected length >= 20")
    }

    @Test
    fun testFormatHwpxStandalone() = runBlocking {
        // Standalone HWPX extraction using extract_bytes_sync
        val input = MAPPER.readValue("{\"data\":\"hwpx/simple.hwpx\",\"mime_type\":\"application/haansofthwpx\"}", ExtractionConfig::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
        assertTrue(result.content.length >= 20, "expected length >= 20")
        assertTrue(result.content.contains("Hello from HWPX"), "expected to contain: " + "Hello from HWPX")
    }

    @Test
    fun testFormatPdfText() = runBlocking {
        // Standalone PDF text extraction using extract_bytes_sync
        val input = MAPPER.readValue("{\"data\":\"pdf/fake_memo.pdf\",\"mime_type\":\"application/pdf\"}", ExtractionConfig::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
        assertTrue(result.content.length >= 50, "expected length >= 50")
        assertTrue(result.content.contains("Mallori") || result.content.contains("May"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testFormatPptx() = runBlocking {
        // PPTX presentation extraction using extract_file_sync
        val input = MAPPER.readValue("{\"mime_type\":\"application/vnd.openxmlformats-officedocument.presentationml.presentation\",\"path\":\"pptx/simple.pptx\"}", ExtractionConfig::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
    }

    @Test
    fun testFormatXlsx() = runBlocking {
        // XLSX spreadsheet extraction using extract_file_sync
        val input = MAPPER.readValue("{\"mime_type\":\"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet\",\"path\":\"xlsx/stanley_cups.xlsx\"}", ExtractionConfig::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
    }

}
