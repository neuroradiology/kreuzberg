// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cb611d287dae325795fe0a711bc52eb1b09a488127c27421b1f05b7a6b932761
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile } from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("validation", () => {
  it("validate_confidence_fn_invalid: Reject confidence threshold > 1.0", async () => {
    await expect(async () => {
      await extractFile(null);
    }).rejects.toThrow();
  });

  it("validate_confidence_fn_valid: Accept valid confidence threshold", async () => {
    await extractFile(null);
  });

  it("validate_format_invalid: Reject invalid output format", async () => {
    await expect(async () => {
      await extractFile(null);
    }).rejects.toThrow();
  });

  it("validate_format_valid: Accept valid output format", async () => {
    await extractFile(null);
  });

  it("validate_host_valid: Accept valid host address", async () => {
    await extractFile(null);
  });

  it("validate_lang_invalid: Reject invalid language code", async () => {
    await expect(async () => {
      await extractFile(null);
    }).rejects.toThrow();
  });

  it("validate_lang_valid: Accept valid language code", async () => {
    await extractFile(null);
  });

  it("validate_mime_invalid: Reject invalid MIME type", async () => {
    await expect(async () => {
      await extractFile(null, "invalid");
    }).rejects.toThrow();
  });

  it("validate_mime_valid: Accept valid MIME type", async () => {
    await extractFile(null, "application/pdf");
  });

  it("validate_oem_invalid: Reject invalid Tesseract OEM mode", async () => {
    await expect(async () => {
      await extractFile(null);
    }).rejects.toThrow();
  });

  it("validate_oem_valid: Accept valid Tesseract OEM mode", async () => {
    await extractFile(null);
  });

  it("validate_port_invalid: Reject invalid port number (zero)", async () => {
    await expect(async () => {
      await extractFile(null);
    }).rejects.toThrow();
  });

  it("validate_port_valid: Accept valid port number", async () => {
    await extractFile(null);
  });

  it("validate_psm_invalid: Reject invalid Tesseract PSM mode", async () => {
    await expect(async () => {
      await extractFile(null);
    }).rejects.toThrow();
  });

  it("validate_psm_valid: Accept valid Tesseract PSM mode", async () => {
    await extractFile(null);
  });
});
