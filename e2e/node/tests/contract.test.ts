// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c39dc9eda1096e5908f5d3623329d139e738e2f60f478cd21a1d3d23c7132388
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";
import { extract, ExtractInput, type ExtractionConfig } from "@xberg/node";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("contract", () => {

	it("api_batch_bytes_async: Tests async batch bytes extraction API (batch_extract_bytes)", async () => {
		const result = await extract({ path: "pdf/fake_memo.pdf" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
	}, 30000);
	it("api_batch_bytes_with_configs_async: Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)", async () => {
		const result = await extract({ config: { outputFormat: "markdown" }, path: "pdf/fake_memo.pdf" } as ExtractionConfig, { outputFormat: "markdown" } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
	}, 30000);
	it("api_batch_file_async: Tests async batch file extraction API (batch_extract_file)", async () => {
		const result = await extract({ path: "pdf/fake_memo.pdf" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
	}, 30000);
	it("api_batch_file_with_configs_async: Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)", async () => {
		const result = await extract({ config: { outputFormat: "markdown" }, path: "pdf/fake_memo.pdf" } as ExtractionConfig, { outputFormat: "markdown" } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
	}, 30000);
	it("api_extract_bytes_async: Tests async bytes extraction API (extract_bytes)", async () => {
		const result = await extract({ path: "pdf/fake_memo.pdf" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
	}, 30000);
	it("api_extract_file_async: Tests async file extraction API (extract_file)", async () => {
		const result = await extract({ path: "pdf/fake_memo.pdf" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["May 5, 2023", "Mallori"].some((v) => result.content.includes(v))).toBe(true);
	}, 30000);
	it("config_document_structure_with_headings: Tests document structure with DOCX heading-driven nesting", async () => {
		const result = await extract({ config: { includeDocumentStructure: true }, path: "docx/fake.docx" } as ExtractionConfig, { includeDocumentStructure: true } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/vnd.openxmlformats-officedocument.wordprocessingml.document");
    {
        const _v = result.document;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.document.nodes.length).toBeGreaterThanOrEqual(1);
	}, 30000);
	it("config_element_types: Tests element-based result format with element type assertions on DOCX", async () => {
		const result = await extract({ config: { resultFormat: "element_based" }, path: "docx/unit_test_headers.docx" } as ExtractionConfig, { resultFormat: "element_based" } as ExtractionConfig);
    expect(["application/vnd.openxmlformats-officedocument.wordprocessingml.document"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.elements.length).toBeGreaterThanOrEqual(1);
	}, 30000);
	it("config_extraction_timeout: Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions", async () => {
		const result = await extract({ config: { extractionTimeoutSecs: 300 }, path: "pdf/fake_memo.pdf" } as ExtractionConfig, { extractionTimeoutSecs: 300 } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
	}, 30000);
	it("config_keywords: Tests keyword extraction via YAKE algorithm", async () => {
		const result = await extract({ config: { keywords: { algorithm: "yake", maxKeywords: 10 } }, path: "pdf/fake_memo.pdf" } as ExtractionConfig, { keywords: { algorithm: "yake", maxKeywords: 10 } } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    // skipped: field 'keywords' not available on Node JsProcessingResult
    // skipped: field 'keywords' not available on Node JsProcessingResult
	}, 30000);
	it("config_pages: Tests page extraction and page marker configuration", async () => {
		const result = await extract({ config: { pages: { extractPages: true, insertPageMarkers: true } }, path: "pdf/fake_memo.pdf" } as ExtractionConfig, { pages: { extractPages: true, insertPageMarkers: true } } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["PAGE"].some((v) => result.content.includes(v))).toBe(true);
	}, 30000);
	it("config_quality_enabled: Tests quality scoring produces a score value in [0.0, 1.0]", async () => {
		const result = await extract({ config: { enableQualityProcessing: true }, path: "pdf/fake_memo.pdf" } as ExtractionConfig, { enableQualityProcessing: true } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    {
        const _v = result.qualityScore;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.qualityScore).toBeGreaterThanOrEqual(0.0);
    expect(result.qualityScore).toBeLessThanOrEqual(1.0);
	}, 30000);
	it("config_security_limits: Tests archive extraction with custom security limits", async () => {
		const result = await extract({ config: { securityLimits: { maxArchiveSize: 104857600, maxCompressionRatio: 50, maxFilesInArchive: 100 } }, path: "archives/documents.zip" } as ExtractionConfig, { securityLimits: { maxArchiveSize: 104857600, maxCompressionRatio: 50, maxFilesInArchive: 100 } } as ExtractionConfig);
    expect(["application/zip", "application/x-zip-compressed"].some((v) => result.mimeType.includes(v))).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
	}, 30000);
	it("config_tree_sitter: Tests tree-sitter configuration round-trip", async () => {
		const result = await extract({ config: { treeSitter: { groups: ["web"], languages: ["python", "rust"], process: { comments: false, diagnostics: false, docstrings: false, exports: true, imports: true, structure: true, symbols: false } } }, path: "code/hello.py" } as ExtractionConfig, { treeSitter: { groups: ["web"], languages: ["python", "rust"], process: { comments: false, diagnostics: false, docstrings: false, exports: true, imports: true, structure: true, symbols: false } } } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("text/x-source-code");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
	}, 30000);
	it("output_format_bytes_markdown: Tests markdown output format via bytes extraction API", async () => {
		const result = await extract({ config: { outputFormat: "markdown" }, data: "pdf/fake_memo.pdf", mimeType: "application/pdf" } as ExtractionConfig, { outputFormat: "markdown" } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
	}, 30000);
	it("output_format_markdown: Tests Markdown output format", async () => {
		const result = await extract({ config: { outputFormat: "markdown" }, path: "pdf/fake_memo.pdf" } as ExtractionConfig, { outputFormat: "markdown" } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((result.metadata.outputFormat ?? "").trim()).toBe("markdown");
	}, 30000);
});
