// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cea3e08dcdd48ba1aecc2622dc48365794f79c33d20158d520ccf5f633e0640b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile } from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("email", () => {
  it("email_eml_html_body: EML with HTML body content.", async () => {
    const result = await extractFile("email/html_only.eml");
    expect(result.mimeType.trim()).toBe("message/rfc822");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("email_eml_multipart: EML with multipart MIME content.", async () => {
    const result = await extractFile("email/html_email_multipart.eml");
    expect(result.mimeType.trim()).toBe("message/rfc822");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("email_eml_utf16: UTF-16 encoded EML file with BOM.", async () => {
    const result = await extractFile("vendored/unstructured/eml/fake-email-utf-16.eml");
    expect(result.mimeType.trim()).toBe("message/rfc822");
    expect(result.content.length).toBeGreaterThanOrEqual(50);
    expect(["Test Email", "Roses are red"].some((v) => result.content.includes(v))).toBe(true);
  });

  it("email_msg_basic: Outlook MSG file extraction.", async () => {
    const result = await extractFile("email/fake_email.msg");
    expect(result.mimeType.trim()).toBe("application/vnd.ms-outlook");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("email_pst_empty: Empty Outlook PST archive with no messages.", async () => {
    const result = await extractFile("email/empty.pst");
    expect(result.mimeType.trim()).toBe("application/vnd.ms-outlook-pst");
  });

  it("email_sample_eml: Sample EML email file to verify email parsing.", async () => {
    const result = await extractFile("email/sample_email.eml");
    expect(result.mimeType.trim()).toBe("message/rfc822");
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });
});
