// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:58d81c724d54895cd37aceda7aa9a20ee2e7e330e592760fea5bda9a0ab20d74
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile } from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("html", () => {
  it("html_complex_layout: Large Wikipedia HTML page to validate complex conversion.", async () => {
    const result = await extractFile("html/taylor_swift.html");
    expect(result.mimeType.trim()).toBe("text/html");
    expect(result.content.length).toBeGreaterThanOrEqual(1000);
  });

  it("html_simple_table: HTML table converted to markdown should retain structure.", async () => {
    const result = await extractFile("html/simple_table.html");
    expect(result.mimeType.trim()).toBe("text/html");
    expect(result.content.length).toBeGreaterThanOrEqual(100);
    expect(result.content).toContain("Product");
    expect(result.content).toContain("Category");
    expect(result.content).toContain("Price");
    expect(result.content).toContain("Stock");
    expect(result.content).toContain("Laptop");
    expect(result.content).toContain("Electronics");
    expect(result.content).toContain("Sample Data Table");
  });
});
