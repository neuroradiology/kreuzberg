// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:93ceeb951f796403f98b1756d4a17d38a1d1bbb49d8fb008986daab64e7df40c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, extractFileSync, , type JsExtractionConfig } from 'kreuzberg';

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

describe('token_reduction', () => {
  it('token_reduction_aggressive: Tests aggressive token reduction mode significantly reduces content', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { tokenReduction: { mode: "aggressive" } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
    expect(result.content.length).toBeLessThanOrEqual(150);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('token_reduction_basic: Tests basic token reduction on PDF document', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { tokenReduction: { mode: "moderate" } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
    expect(result.content.length).toBeLessThanOrEqual(200);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('token_reduction_light: Tests light token reduction mode preserves most content', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { tokenReduction: { mode: "light" } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.content.length).toBeGreaterThan(0);
  });

  it('token_reduction_reduce_aggressive: Reduce tokens with aggressive compression level', () => {
    const result = ({ config: { level: "aggressive" }, text: "The quick brown fox jumps over the lazy dog." });
    expect(result.result.length).toBeGreaterThanOrEqual(1);
  });

  it('token_reduction_reduce_basic: Reduce tokens with light compression level', () => {
    const result = ({ config: { level: "light" }, text: "The quick brown fox jumps over the lazy dog. This is a test sentence." });
    expect(result.result).toContain("quick");
  });

  it('token_reduction_with_chunking: Tests token reduction combined with chunking', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { chunking: { maxCharacters: 500, overlap: 50 }, tokenReduction: { mode: "moderate" } } as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
    expect(result.content.length).toBeLessThanOrEqual(200);
    expect(result.content.length).toBeGreaterThan(0);
    expect(result.chunks.length).toBeGreaterThanOrEqual(1);
    expect((result.chunks ?? []).every((c: { content?: string }) => !!c.content)).toBe(true);
  });
});
