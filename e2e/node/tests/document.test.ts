// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3823564a5e1730fd5dc60391a37ef78ec49fdc21513ea7cf53d94f786c6e8595
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile, extractFileSync, type JsExtractionConfig } from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("document", () => {
  it("doc_metadata_docx: Extract DOCX metadata", () => {
    const result = extractFileSync("vendored/markitdown/test.docx");
    expect(result.metadata.length).toBeGreaterThan(0);
  });

  it("doc_metadata_pdf: Extract PDF metadata", () => {
    const result = extractFileSync("pdf/fake_memo.pdf");
    expect(result.metadata.length).toBeGreaterThan(0);
  });

  it("doc_structure_docx: Extract DOCX document structure with include_document_structure config", () => {
    const result = extractFileSync("vendored/markitdown/test.docx", undefined, {
      includeDocumentStructure: true,
    } as JsExtractionConfig);
    expect((result.document ?? "").length).toBeGreaterThan(0);
  });

  it("doc_structure_html: Extract HTML document structure with include_document_structure config", () => {
    const result = extractFileSync("html/html.html", undefined, {
      includeDocumentStructure: true,
    } as JsExtractionConfig);
    expect((result.document ?? "").length).toBeGreaterThan(0);
  });

  it("doc_structure_pdf: Extract PDF document structure with include_document_structure config", () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, {
      includeDocumentStructure: true,
    } as JsExtractionConfig);
    expect((result.document ?? "").length).toBeGreaterThan(0);
  });
});
