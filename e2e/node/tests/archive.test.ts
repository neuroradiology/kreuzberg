// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1f2b4a2f6f2d00adeb17abbdadaf992f01482256e1596b3a0704abf406f1eaf3
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile } from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("archive", () => {
  it("archive_gz_basic: Gzip compressed file extraction.", async () => {
    const result = await extractFile("archives/book_war_and_peace_1p.txt.gz");
    expect(
      ["application/gzip", "application/x-gzip"].some((v) => result.mimeType.includes(v)),
    ).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("archive_sevenz_basic: 7-Zip archive extraction.", async () => {
    const result = await extractFile("archives/documents.7z");
    expect(result.mimeType.trim()).toBe("application/x-7z-compressed");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("archive_tar_basic: TAR archive extraction.", async () => {
    const result = await extractFile("archives/documents.tar");
    expect(["application/x-tar", "application/tar"].some((v) => result.mimeType.includes(v))).toBe(
      true,
    );
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("archive_zip_basic: ZIP archive extraction.", async () => {
    const result = await extractFile("archives/documents.zip");
    expect(
      ["application/zip", "application/x-zip-compressed"].some((v) => result.mimeType.includes(v)),
    ).toBe(true);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });
});
