// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c39dc9eda1096e5908f5d3623329d139e738e2f60f478cd21a1d3d23c7132388
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";
import { extract, ExtractInput, type ExtractionConfig } from "@xberg/node";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("smoke", () => {

	it("ocr_image_png: OCR: PNG image extraction with OCR enabled. In WASM this exercises the Uint8Array bridge parameter and Promise await in the generated OcrBackend bridge.", async () => {
		const result = await extract({ config: {  }, data: "images/test_hello_world.png", mimeType: "image/png" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("image/png");
    expect(result.content.length).toBeGreaterThanOrEqual(1);
    expect(["Hello", "World", "hello", "world"].some((v) => result.content.includes(v))).toBe(true);
	}, 30000);
	it("smoke_docx_basic: Smoke test: DOCX with formatted text", async () => {
		const result = await extract({ config: {  }, mimeType: "application/vnd.openxmlformats-officedocument.wordprocessingml.document", path: "docx/fake.docx" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("application/vnd.openxmlformats-officedocument.wordprocessingml.document");
    expect(result.content.length).toBeGreaterThanOrEqual(20);
    expect(["Lorem", "ipsum", "document", "text"].some((v) => result.content.includes(v))).toBe(true);
	}, 30000);
	it("smoke_html_basic: Smoke test: HTML table extraction", async () => {
		const result = await extract({ config: {  }, mimeType: "text/html", path: "html/simple_table.html" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("text/html");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(["Sample Data Table", "Laptop", "Electronics", "Product"].some((v) => result.content.includes(v))).toBe(true);
	}, 30000);
	it("smoke_image_png: Smoke test: PNG image (without OCR, metadata only)", async () => {
		const result = await extract({ config: { disableOcr: true }, mediaType: "image/png", path: "images/sample.png" } as ExtractionConfig, { disableOcr: true } as ExtractionConfig);
    expect(result.mimeType.trim()).toBe("image/png");
	}, 30000);
	it("smoke_json_basic: Smoke test: JSON file extraction", async () => {
		const result = await extract({ config: {  }, mimeType: "application/json", path: "json/simple.json" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("application/json");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
	}, 30000);
	it("smoke_pdf_basic: Smoke test: PDF with simple text extraction", async () => {
		const result = await extract({ config: {  }, mimeType: "application/pdf", path: "pdf/fake_memo.pdf" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(50);
    expect(["May 5, 2023", "To Whom it May Concern"].some((v) => result.content.includes(v))).toBe(true);
	}, 30000);
	it("smoke_txt_basic: Smoke test: Plain text file", async () => {
		const result = await extract({ config: {  }, mimeType: "text/plain", path: "text/report.txt" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("text/plain");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
	}, 30000);
	it("smoke_xlsx_basic: Smoke test: XLSX with basic spreadsheet data including tables", async () => {
		const result = await extract({ config: {  }, mimeType: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", path: "xlsx/stanley_cups.xlsx" } as ExtractionConfig, undefined);
    expect(result.mimeType.trim()).toBe("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
    expect(result.content.length).toBeGreaterThanOrEqual(100);
    expect(result.content).toContain("Team");
    expect(result.content).toContain("Location");
    expect(result.content).toContain("Stanley Cups");
    expect(result.content).toContain("Blues");
    expect(result.content).toContain("Flyers");
    expect(result.content).toContain("Maple Leafs");
    expect(result.content).toContain("STL");
    expect(result.content).toContain("PHI");
    expect(result.content).toContain("TOR");
    expect(result.tables.length).toBeGreaterThanOrEqual(1);
    expect(result.metadata.format.excel.sheetCount).toBeGreaterThanOrEqual(2);
    expect(result.metadata.format.excel.sheetNames.some((item) => _alefE2eItemTexts(item).some((text) => text.includes("Stanley Cups")))).toBe(true);
	}, 30000);
});
