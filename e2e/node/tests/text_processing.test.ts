// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e6fc2266e3def7ec10f429329b1f5753a4199f95716be67ca1e88e755628a457
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile, extractFileSync } from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("text_processing", () => {
  it("text_clean_extracted: Extract and clean text content from document", () => {
    const result = extractFileSync("json/simple.json");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it("text_normalize_whitespace: Extract text with whitespace normalization (no consecutive spaces)", () => {
    const result = extractFileSync("pdf/fake_memo.pdf");
    expect(result.content).not.toContain("  ");
  });
});
