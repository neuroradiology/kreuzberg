// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ddd5a433e76114f60c6f5d24a40bf5061dac9988b9bba7b3047436e92f419621
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile } from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("markup", () => {
  it("bold_basic: Create bold markdown formatting", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(9);
  });

  it("bold_empty: Bold empty string", async () => {
    await extractFile(null);
  });

  it("bold_special_chars: Bold special chars", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(23);
  });

  it("code_basic: Create inline code formatting", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(8);
  });

  it("code_multiline: Code multiline", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(11);
  });

  it("italic_basic: Create italic markdown formatting", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(8);
  });

  it("italic_empty: Italic empty string", async () => {
    await extractFile(null);
  });

  it("italic_special_chars: Italic special chars", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(17);
  });

  it("link_basic: Create markdown link with text and URL", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(10);
  });

  it("link_empty_text: Create markdown link with empty text", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(0);
  });

  it("link_special_url: Link with query params", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(5);
  });

  it("strikethrough_basic: Create strikethrough formatting", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(7);
  });

  it("strikethrough_empty: Strikethrough empty", async () => {
    await extractFile(null);
  });

  it("underline_basic: Create underline formatting", async () => {
    const result = await extractFile(null);
    expect(result.start).toBe(0);
    expect(result.end).toBe(10);
  });

  it("underline_empty: Underline empty", async () => {
    await extractFile(null);
  });
});
