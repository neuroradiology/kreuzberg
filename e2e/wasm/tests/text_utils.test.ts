// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:db4e7fe7640c36387e192aa3a544778d035e42197120afa3ad2fadb7bfa73b28
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile } from "kreuzberg";

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("text_utils", () => {
  it("clean_text_basic: Remove null characters from text", async () => {
    const result = await extractFile(null);
    expect(result.result).toContain("Hello");
  });

  it("clean_text_whitespace: Normalize whitespace in extracted text", async () => {
    const result = await extractFile(null);
    expect(result.result.length).toBeGreaterThanOrEqual(1);
  });

  it("dedup_basic: Deduplicate repeated lines", () => {
    const result = { texts: ["line1", "line1", "line2", "line2", "line3"] };
    expect(result.result).toContain("line1");
  });

  it("dedup_unique: Deduplicate text with all unique lines", () => {
    const result = { texts: ["a", "b", "c"] };
    expect(result.result).toContain("a");
  });

  it("escape_html_ampersand: Escape ampersands in HTML text", async () => {
    const result = await extractFile(null);
    expect(result.result).toContain("&amp;");
  });

  it("escape_html_basic: Escape HTML special characters including angle brackets", async () => {
    const result = await extractFile(null);
    expect(result.result).toContain("&lt;");
  });

  it("fix_mojibake_basic: Fix mojibake encoding issues", async () => {
    const result = await extractFile(null);
    expect(result.result.length).toBeGreaterThanOrEqual(1);
  });

  it("fix_mojibake_clean: Clean text passes through", async () => {
    const result = await extractFile(null);
    expect(result.result).toContain("Normal ASCII text");
  });

  it("fix_mojibake_utf8: Fix mojibake encoding", async () => {
    await extractFile(null);
  });

  it("is_utf8_ascii: Validate ASCII text as valid UTF-8", async () => {
    const result = await extractFile(null);
    expect(result.result).toBe(true);
  });

  it("is_utf8_valid: Validate UTF-8 encoding", async () => {
    const result = await extractFile(null);
    expect(result.result).toBe(true);
  });

  it("normalize_whitespace_basic: Normalize multiple spaces and newlines", () => {
    const result = { text: "hello   world\n\n\nfoo" };
    expect(result.result).toContain("hello");
  });

  it("normalize_whitespace_tabs: Normalize tabs to spaces", () => {
    const result = { text: "col1\t\tcol2\t\tcol3" };
    expect(result.result).toContain("col1");
  });
});
