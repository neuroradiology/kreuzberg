# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c39dc9eda1096e5908f5d3623329d139e738e2f60f478cd21a1d3d23c7132388
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: format_specific
defmodule E2e.FormatSpecificTest do
  use ExUnit.Case, async: false

  describe "format_docx_standalone" do
    test "format_docx_standalone" do
      {:ok, result} = Xberg.extract_async("{\"data\":\"docx/fake.docx\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.wordprocessingml.document\"}")
      assert (is_binary(result.content) && byte_size(result.content) >= 20) || (is_list(result.content) && length(result.content) >= 20)
    end
  end

  describe "format_hwpx_standalone" do
    test "format_hwpx_standalone" do
      {:ok, result} = Xberg.extract_async("{\"data\":\"hwpx/simple.hwpx\",\"mime_type\":\"application/haansofthwpx\"}")
      assert (is_binary(result.content) && byte_size(result.content) >= 20) || (is_list(result.content) && length(result.content) >= 20)
      assert String.contains?(to_string(result.content), "Hello from HWPX")
    end
  end

  describe "format_pdf_text" do
    test "format_pdf_text" do
      {:ok, result} = Xberg.extract_async("{\"data\":\"pdf/fake_memo.pdf\",\"mime_type\":\"application/pdf\"}")
      assert (is_binary(result.content) && byte_size(result.content) >= 50) || (is_list(result.content) && length(result.content) >= 50)
      assert Enum.any?(["Mallori", "May"], fn v -> String.contains?(to_string(result.content), v) end)
    end
  end

  describe "format_pptx" do
    test "format_pptx" do
      {:ok, result} = Xberg.extract_async("{\"mime_type\":\"application/vnd.openxmlformats-officedocument.presentationml.presentation\",\"path\":\"pptx/simple.pptx\"}")
    end
  end

  describe "format_xlsx" do
    test "format_xlsx" do
      {:ok, result} = Xberg.extract_async("{\"mime_type\":\"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet\",\"path\":\"xlsx/stanley_cups.xlsx\"}")
    end
  end
end
