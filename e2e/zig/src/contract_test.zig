// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c39dc9eda1096e5908f5d3623329d139e738e2f60f478cd21a1d3d23c7132388
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
const std = @import("std");
const testing = std.testing;
const xberg = @import("xberg");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

// E2e tests for category: contract

test "api_batch_bytes_async" {
    // Tests async batch bytes extraction API (batch_extract_bytes)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"path\":\"pdf/fake_memo.pdf\"}", "{}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expect(
        std.mem.indexOf(u8, result.object.get("content").?.string, "May 5, 2023") != null or
        std.mem.indexOf(u8, result.object.get("content").?.string, "Mallori") != null
    );
}

test "api_batch_bytes_with_configs_async" {
    // Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"output_format\":\"markdown\"},\"path\":\"pdf/fake_memo.pdf\"}", "{\"output_format\":\"markdown\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expectEqualStrings("markdown", std.mem.trim(u8, result.object.get("metadata").?.object.get("output_format").?.string, " \n\r\t"));
}

test "api_batch_file_async" {
    // Tests async batch file extraction API (batch_extract_file)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"path\":\"pdf/fake_memo.pdf\"}", "{}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expect(
        std.mem.indexOf(u8, result.object.get("content").?.string, "May 5, 2023") != null or
        std.mem.indexOf(u8, result.object.get("content").?.string, "Mallori") != null
    );
}

test "api_batch_file_with_configs_async" {
    // Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"output_format\":\"markdown\"},\"path\":\"pdf/fake_memo.pdf\"}", "{\"output_format\":\"markdown\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expectEqualStrings("markdown", std.mem.trim(u8, result.object.get("metadata").?.object.get("output_format").?.string, " \n\r\t"));
}

test "api_extract_bytes_async" {
    // Tests async bytes extraction API (extract_bytes)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"path\":\"pdf/fake_memo.pdf\"}", "{}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expect(
        std.mem.indexOf(u8, result.object.get("content").?.string, "May 5, 2023") != null or
        std.mem.indexOf(u8, result.object.get("content").?.string, "Mallori") != null
    );
}

test "api_extract_file_async" {
    // Tests async file extraction API (extract_file)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"path\":\"pdf/fake_memo.pdf\"}", "{}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expect(
        std.mem.indexOf(u8, result.object.get("content").?.string, "May 5, 2023") != null or
        std.mem.indexOf(u8, result.object.get("content").?.string, "Mallori") != null
    );
}

test "config_document_structure_with_headings" {
    // Tests document structure with DOCX heading-driven nesting
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"include_document_structure\":true},\"path\":\"docx/fake.docx\"}", "{\"include_document_structure\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/vnd.openxmlformats-officedocument.wordprocessingml.document", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("document").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").?.array.items.len >= @as(usize, 1));
}

test "config_element_types" {
    // Tests element-based result format with element type assertions on DOCX
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"result_format\":\"element_based\"},\"path\":\"docx/unit_test_headers.docx\"}", "{\"result_format\":\"element_based\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(
        std.mem.indexOf(u8, result.object.get("mime_type").?.string, "application/vnd.openxmlformats-officedocument.wordprocessingml.document") != null
    );
    try testing.expect(result.object.get("elements").?.array.items.len >= @as(usize, 1));
}

test "config_extraction_timeout" {
    // Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"extraction_timeout_secs\":300},\"path\":\"pdf/fake_memo.pdf\"}", "{\"extraction_timeout_secs\":300}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
}

test "config_keywords" {
    // Tests keyword extraction via YAKE algorithm
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"keywords\":{\"algorithm\":\"yake\",\"max_keywords\":10}},\"path\":\"pdf/fake_memo.pdf\"}", "{\"keywords\":{\"algorithm\":\"yake\",\"max_keywords\":10}}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    // skipped: field 'keywords' not available on the JSON-struct result
    // skipped: field 'keywords' not available on the JSON-struct result
}

test "config_pages" {
    // Tests page extraction and page marker configuration
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"pages\":{\"extract_pages\":true,\"insert_page_markers\":true}},\"path\":\"pdf/fake_memo.pdf\"}", "{\"pages\":{\"extract_pages\":true,\"insert_page_markers\":true}}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expect(
        std.mem.indexOf(u8, result.object.get("content").?.string, "PAGE") != null
    );
}

test "config_quality_enabled" {
    // Tests quality scoring produces a score value in [0.0, 1.0]
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"enable_quality_processing\":true},\"path\":\"pdf/fake_memo.pdf\"}", "{\"enable_quality_processing\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expect(result.object.get("quality_score").? != .null);
    try testing.expect(result.object.get("quality_score").?.float >= @as(f64, 0.0));
    try testing.expect(result.object.get("quality_score").?.float <= @as(f64, 1.0));
}

test "config_security_limits" {
    // Tests archive extraction with custom security limits
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"security_limits\":{\"max_archive_size\":104857600,\"max_compression_ratio\":50,\"max_files_in_archive\":100}},\"path\":\"archives/documents.zip\"}", "{\"security_limits\":{\"max_archive_size\":104857600,\"max_compression_ratio\":50,\"max_files_in_archive\":100}}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(
        std.mem.indexOf(u8, result.object.get("mime_type").?.string, "application/zip") != null or
        std.mem.indexOf(u8, result.object.get("mime_type").?.string, "application/x-zip-compressed") != null
    );
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
}

test "config_tree_sitter" {
    // Tests tree-sitter configuration round-trip
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"tree_sitter\":{\"groups\":[\"web\"],\"languages\":[\"python\",\"rust\"],\"process\":{\"comments\":false,\"diagnostics\":false,\"docstrings\":false,\"exports\":true,\"imports\":true,\"structure\":true,\"symbols\":false}}},\"path\":\"code/hello.py\"}", "{\"tree_sitter\":{\"groups\":[\"web\"],\"languages\":[\"python\",\"rust\"],\"process\":{\"comments\":false,\"diagnostics\":false,\"docstrings\":false,\"exports\":true,\"imports\":true,\"structure\":true,\"symbols\":false}}}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("text/x-source-code", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 5));
}

test "output_format_bytes_markdown" {
    // Tests markdown output format via bytes extraction API
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"output_format\":\"markdown\"},\"data\":\"pdf/fake_memo.pdf\",\"mime_type\":\"application/pdf\"}", "{\"output_format\":\"markdown\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expectEqualStrings("markdown", std.mem.trim(u8, result.object.get("metadata").?.object.get("output_format").?.string, " \n\r\t"));
}

test "output_format_markdown" {
    // Tests Markdown output format
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try xberg.extract("{\"config\":{\"output_format\":\"markdown\"},\"path\":\"pdf/fake_memo.pdf\"}", "{\"output_format\":\"markdown\"}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("application/pdf", std.mem.trim(u8, result.object.get("mime_type").?.string, " \n\r\t"));
    try testing.expect(result.object.get("content").?.string.len >= @as(usize, 10));
    try testing.expectEqualStrings("markdown", std.mem.trim(u8, result.object.get("metadata").?.object.get("output_format").?.string, " \n\r\t"));
}
