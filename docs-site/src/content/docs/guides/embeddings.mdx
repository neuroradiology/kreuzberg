---
title: "Embeddings"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { Content as Snip_python_utils_embedding_with_chunking } from "../../../snippets/python/utils/embedding_with_chunking.md";
import { Content as Snip_typescript_utils_embedding_with_chunking } from "../../../snippets/typescript/utils/embedding_with_chunking.md";
import { Content as Snip_rust_advanced_embedding_with_chunking } from "../../../snippets/rust/advanced/embedding_with_chunking.md";
import { Content as Snip_go_advanced_embedding_with_chunking } from "../../../snippets/go/advanced/embedding_with_chunking.md";
import { Content as Snip_java_advanced_embedding_with_chunking } from "../../../snippets/java/advanced/embedding_with_chunking.md";
import { Content as Snip_csharp_advanced_embedding_with_chunking } from "../../../snippets/csharp/advanced/embedding_with_chunking.md";
import { Content as Snip_ruby_advanced_embedding_with_chunking } from "../../../snippets/ruby/advanced/embedding_with_chunking.md";
import { Content as Snip_python_utils_standalone_embed } from "../../../snippets/python/utils/standalone_embed.md";
import { Content as Snip_typescript_utils_standalone_embed } from "../../../snippets/typescript/utils/standalone_embed.md";
import { Content as Snip_rust_utils_standalone_embed } from "../../../snippets/rust/utils/standalone_embed.md";
import { Content as Snip_go_utils_standalone_embed } from "../../../snippets/go/utils/standalone_embed.md";
import { Content as Snip_java_utils_standalone_embed } from "../../../snippets/java/utils/standalone_embed.md";
import { Content as Snip_csharp_utils_standalone_embed } from "../../../snippets/csharp/utils/standalone_embed.md";
import { Content as Snip_ruby_utils_standalone_embed } from "../../../snippets/ruby/utils/standalone_embed.md";
import { Content as Snip_python_utils_vector_database_integration } from "../../../snippets/python/utils/vector_database_integration.md";
import { Content as Snip_typescript_utils_vector_database_integration } from "../../../snippets/typescript/utils/vector_database_integration.md";
import { Content as Snip_rust_advanced_vector_database_integration } from "../../../snippets/rust/advanced/vector_database_integration.md";
import { Content as Snip_go_advanced_vector_database_integration } from "../../../snippets/go/advanced/vector_database_integration.md";
import { Content as Snip_java_advanced_vector_database_integration } from "../../../snippets/java/advanced/vector_database_integration.md";
import { Content as Snip_csharp_advanced_vector_database_integration } from "../../../snippets/csharp/advanced/vector_database_integration.md";
import { Content as Snip_ruby_advanced_vector_database_integration } from "../../../snippets/ruby/advanced/vector_database_integration.md";

Turn extracted text into vectors for semantic search and RAG, using local ONNX models or a registered backend — no external API calls. Enable the `embeddings` feature to use in-process embedding backends.

| Preset         | Model                        | Dimensions | Chunk Size (chars) | Use Case                                                |
| -------------- | ---------------------------- | ---------- | ------------------ | ------------------------------------------------------- |
| `fast`         | all-MiniLM-L6-v2 (quantized) | 384        | 512                | Quick prototyping, development, resource-constrained    |
| `balanced`     | BGE-base-en-v1.5             | 768        | 1024               | General-purpose RAG, production deployments, English    |
| `quality`      | BGE-large-en-v1.5            | 1024       | 2000               | Complex documents, maximum accuracy, sufficient compute |
| `multilingual` | multilingual-e5-base         | 768        | 1024               | International documents, mixed-language content         |

The chunk-size column is the preset's target chunk size in characters (its `chunk_size`), not a token limit.

## Model Types

Select the embedding source via the `model` field of `EmbeddingConfig`. `EmbeddingModelType` has four variants, tagged by `type`:

- `preset` — a bundled ONNX model configuration by `name` (see the preset table above). Recommended default.
- `custom` — any ONNX embedding model from HuggingFace, given its `model_id` and output `dimensions`.
- `llm` — a provider-hosted embedding model through liter-llm, configured by a nested `llm` (`LlmConfig`), e.g. `openai/text-embedding-3-small`.
- `plugin` — an in-process backend registered by `name` via the plugin system (see below).

## In-Process Embedding Backends (Plugin Variant)

Plug a caller-managed embedder (e.g. `llama-cpp-python`, `sentence-transformers`) into Xberg via the `Plugin` variant of `EmbeddingModelType` — Xberg calls back into the registered backend instead of running its own ONNX model.

1. Register the backend once at startup via `xberg::plugins::register_embedding_backend(Arc::new(MyEmbedder))`. The backend implements `EmbeddingBackend` (a `Plugin`-inheriting async trait with `dimensions()` and `embed(texts) -> Vec<Vec<f32>>`).
2. Reference it by name in `EmbeddingConfig`: `{ "model": { "type": "plugin", "name": "my-embedder" } }`.
3. Optional: set `EmbeddingConfig.max_embed_duration_secs` (default 60) to bound the wait on a hung backend; `None` disables the timeout.

Rust extraction configuration and `XBERG_EMBEDDING_PLUGIN_NAME` accept the Plugin variant once a backend is registered.

**Fork-safety**: Python callers running under `multiprocessing`, `gunicorn`'s prefork worker, or Celery prefork must re-register the backend in each child process — native-backed embedders (including `llama-cpp-python`) aren't fork-safe. Use `os.register_at_fork(after_in_child=reregister_fn)` to automate the re-registration.

## Configuration

<Tabs syncKey="lang">
<TabItem label="Python">

<Snip_python_utils_embedding_with_chunking />

</TabItem>
<TabItem label="TypeScript">

<Snip_typescript_utils_embedding_with_chunking />

</TabItem>
<TabItem label="Rust">

<Snip_rust_advanced_embedding_with_chunking />

</TabItem>
<TabItem label="Go">

<Snip_go_advanced_embedding_with_chunking />

</TabItem>
<TabItem label="Java">

<Snip_java_advanced_embedding_with_chunking />

</TabItem>
<TabItem label="C#">

<Snip_csharp_advanced_embedding_with_chunking />

</TabItem>
<TabItem label="Ruby">

<Snip_ruby_advanced_embedding_with_chunking />

</TabItem>
</Tabs>

## Standalone Embedding

Call `embed_texts` (or `embed_texts_async`) to embed a list of strings directly with an `EmbeddingConfig`, bypassing extraction and chunking. Each input string maps to one output vector.

<Tabs syncKey="lang">
<TabItem label="Python">

<Snip_python_utils_standalone_embed />

</TabItem>
<TabItem label="TypeScript">

<Snip_typescript_utils_standalone_embed />

</TabItem>
<TabItem label="Rust">

<Snip_rust_utils_standalone_embed />

</TabItem>
<TabItem label="Go">

<Snip_go_utils_standalone_embed />

</TabItem>
<TabItem label="Java">

<Snip_java_utils_standalone_embed />

</TabItem>
<TabItem label="C#">

<Snip_csharp_utils_standalone_embed />

</TabItem>
<TabItem label="Ruby">

<Snip_ruby_utils_standalone_embed />

</TabItem>
</Tabs>

## Vector Database Integration

<Tabs syncKey="lang">
<TabItem label="Python">

<Snip_python_utils_vector_database_integration />

</TabItem>
<TabItem label="TypeScript">

<Snip_typescript_utils_vector_database_integration />

</TabItem>
<TabItem label="Rust">

<Snip_rust_advanced_vector_database_integration />

</TabItem>
<TabItem label="Go">

<Snip_go_advanced_vector_database_integration />

</TabItem>
<TabItem label="Java">

<Snip_java_advanced_vector_database_integration />

</TabItem>
<TabItem label="C#">

<Snip_csharp_advanced_vector_database_integration />

</TabItem>
<TabItem label="Ruby">

<Snip_ruby_advanced_vector_database_integration />

</TabItem>
</Tabs>

## See also

- [Chunking](/guides/chunking/) — split documents before embedding for RAG
- [Configuration Reference](/reference/configuration/#embeddingconfig) — all embedding options
- [LLM Integration](/guides/llm-integration/) — use embeddings with LLMs
