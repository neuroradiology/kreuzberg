/**
 * Generated by alef. Do not edit by hand.
 * alef:hash:c26185e7af68ca4756f10e088470f87071eb21785e05d75acd73c4ee0d7abef9
 *
 * TypeScript trait bridge for outbound plugin implementations.
 */

export interface ValidatorBridge {
  name(): string;
  version(): string;
  initialize(): Promise<void>;
  shutdown(): Promise<void>;
  validate?(result: string, config: string): string;
  shouldValidate(result: string, config: string): string;
  priority(): string;
}

export class ValidatorAdapter {
  constructor(private bridge: ValidatorBridge) {}

  async validate(result: string, config: string): Promise<string> {
    try {
      const result = await this.bridge.validate(result, config);
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

  async shouldValidate(result: string, config: string): Promise<string> {
    try {
      const result = await this.bridge.shouldValidate(result, config);
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

  async priority(): Promise<string> {
    try {
      const result = await this.bridge.priority();
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

}
