// Generated by alef. Do not edit by hand.
// alef:hash:e939c65acd0b9087465d609f7d4a63fb1e21a6ea05b1bfeb8dc1b32b54a13cf6

package dev.kreuzberg

/// Bridge adapter for IPostProcessor implementations.
///
/// Wraps a user-supplied IPostProcessor instance and delegates all method calls.
/// This adapter is used internally by the trait bridge registration layer to ensure
/// proper handling of impl instances through JNI boundaries.
internal class PostProcessorAdapter(private val impl: IPostProcessor) : IPostProcessor {
    // Delegate all interface methods to the wrapped implementation
    override fun name(): String = impl.name()

    override fun version(): String = impl.version()

    override fun initialize() = impl.initialize()

    override fun shutdown() = impl.shutdown()

    override suspend fun process(result: ExtractionResult, config: ExtractionConfig): Unit {
        return impl.process(result, config)
    }

    override fun processingStage(): ProcessingStage {
        return impl.processingStage()
    }

    override fun shouldProcess(result: ExtractionResult, config: ExtractionConfig): Boolean {
        return impl.shouldProcess(result, config)
    }

    override fun estimatedDurationMs(result: ExtractionResult): Long {
        return impl.estimatedDurationMs(result)
    }

    override fun priority(): Int {
        return impl.priority()
    }


}
