// Generated by alef. Do not edit by hand.
// alef:hash:e939c65acd0b9087465d609f7d4a63fb1e21a6ea05b1bfeb8dc1b32b54a13cf6

package dev.kreuzberg

/// Bridge adapter for IValidator implementations.
///
/// Wraps a user-supplied IValidator instance and delegates all method calls.
/// This adapter is used internally by the trait bridge registration layer to ensure
/// proper handling of impl instances through JNI boundaries.
internal class ValidatorAdapter(private val impl: IValidator) : IValidator {
    // Delegate all interface methods to the wrapped implementation
    override fun name(): String = impl.name()

    override fun version(): String = impl.version()

    override fun initialize() = impl.initialize()

    override fun shutdown() = impl.shutdown()

    override suspend fun validate(result: ExtractionResult, config: ExtractionConfig): Unit {
        return impl.validate(result, config)
    }

    override fun shouldValidate(result: ExtractionResult, config: ExtractionConfig): Boolean {
        return impl.shouldValidate(result, config)
    }

    override fun priority(): Int {
        return impl.priority()
    }


}
