# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:52d810dddba1010b606994310ce2a05f1937641c736814d217688096779f4320
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg.EnrichOptions do
  @moduledoc """
  Which enrichment passes to run on a piece of text.

  All fields default to `false` / empty so callers can opt in precisely.

  # Examples

  ```
  use xberg::enrichment::EnrichOptions;

  let opts = EnrichOptions {
      keywords: true,
      ..Default::default()
  };
  assert!(opts.keywords);
  assert!(!opts.entities);
  assert!(opts.labels.is_empty());
  ```
  """

  @typedoc "Which enrichment passes to run on a piece of text."
  @type t :: %__MODULE__{
          keywords: boolean(),
          entities: boolean(),
          labels: [String.t()]
        }

  defstruct keywords: false,
            entities: false,
            labels: []

  defimpl Jason.Encoder do
    @doc false
    def encode(value, opts) do
      value
      |> Map.from_struct()
      |> Enum.reject(fn {_k, v} -> v == nil end)
      |> Enum.into(%{})
      |> Jason.Encoder.encode(opts)
    end
  end
end
