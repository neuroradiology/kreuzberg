# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:d63e0283476bd8f41fd1b07ab7d0bbfde196679db404e32bcf4edb1a021f1f52
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg.SvgOptions do
  @moduledoc """
  SVG-specific configuration for the image-encode pipeline.

  Applies when the source image is SVG or when the output format is set to
  [`ImageOutputFormat::Svg`].  Available when the `svg` feature is active.

  Used via [`ImageExtractionConfig::svg`].
  """

  @typedoc "SVG-specific configuration for the image-encode pipeline."
  @type t :: %__MODULE__{
          sanitize: boolean(),
          render_dpi: float()
        }

  defstruct sanitize: true,
            render_dpi: 96

  defimpl Jason.Encoder do
    @doc false
    def encode(value, opts) do
      value
      |> Map.from_struct()
      |> Enum.reject(fn {_k, v} -> v == nil end)
      |> Enum.into(%{})
      |> Jason.Encoder.encode(opts)
    end
  end
end
