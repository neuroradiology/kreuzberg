# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:fa49f061b4922ae545a884b3557c72331bc9acf91f2b03d4dfc68363dc9f62b9
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Kreuzberg.EnrichResult do
  @moduledoc """
  Structured output produced by a completed enrichment pass.

  Fields are populated only when the corresponding [`EnrichOptions`] flag was set.

  # Examples

  ```
  use kreuzberg::enrichment::EnrichResult;

  let result = EnrichResult::default();
  assert!(result.keywords.is_empty());
  assert!(result.entities.is_empty());
  assert!(result.labels.is_empty());
  ```
  """

  @typedoc "Structured output produced by a completed enrichment pass."
  @type t :: %__MODULE__{
          keywords: [String.t()],
          entities: [map()],
          labels: [String.t()]
        }

  defstruct keywords: [],
            entities: [],
            labels: []

  defimpl Jason.Encoder do
    @doc false
    def encode(value, opts) do
      value
      |> Map.from_struct()
      |> Enum.reject(fn {_k, v} -> v == nil end)
      |> Enum.into(%{})
      |> Jason.Encoder.encode(opts)
    end
  end
end
