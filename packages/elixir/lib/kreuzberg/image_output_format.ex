# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:4c69455352c6694c6639030ae0ef9d405ca032b08d85d13bed84086fcdf60c8d
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Kreuzberg.ImageOutputFormat do
  @moduledoc """
  Target format for re-encoding extracted images.

  Controls whether and how extracted images are normalised to a uniform
  container format before being returned in `ExtractionResult.images`.
  The default (`Native`) preserves the format produced by each extractor
  without any additional encode pass.

  Callers that need uniform output — e.g. cloud pipelines that always store
  WebP thumbnails — set this once on `ImageExtractionConfig.output_format`
  rather than re-encoding downstream.

  # Serde shape

  Uses a tagged enum: `{"type": "native"}`, `{"type": "png"}`,
  `{"type": "jpeg", "quality": 90}`, etc.
  """

  @typedoc "Target format for re-encoding extracted images."
  @type t :: term()

  @typedoc "Preserve whatever format the extractor produced (default)."
  @type native :: :native
  @typedoc "Re-encode all extracted images as PNG (lossless)."
  @type png :: :png
  @typedoc "Re-encode all extracted images as JPEG at the given quality level."
  @type jpeg :: %{type: :jpeg, quality: non_neg_integer()}
  @typedoc "Re-encode all extracted images as WebP at the given quality level."
  @type webp :: %{type: :webp, quality: non_neg_integer()}
  @typedoc "Re-encode all extracted images as HEIF/HEIC at the given quality level."
  @type heif :: %{type: :heif, quality: non_neg_integer()}
end
